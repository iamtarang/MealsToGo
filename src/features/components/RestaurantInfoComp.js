import { Text } from "react-native-paper"
import { View } from "react-native"

const RestaurantInfoComp = () => {
    return (
        <View>RestaurantInfoComp</View>
    )
}

export default RestaurantInfoComp